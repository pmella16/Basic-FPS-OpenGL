#version 330 core
layout (location = 0) in vec3 sunPosition_modelspace;

uniform mat4 MVP;
//uniform mat4 ModelMatrix;

void main()
{
    //gl_Position = ModelMatrix * vec4(sunPosition_modelspace, 1.0);

    gl_Position = MVP * vec4(sunPosition_modelspace, 1.0);
} 